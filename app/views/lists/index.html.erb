<div class="container">
<h1 class="text-center mt-3">Lists</h1>

<div class="container text-center">
<%= link_to "Add a movie list", new_list_path, class: "btn btn-primary m-3" %>
</div>


  <% @lists.each do |list| %>
    <div>
      <%= link_to list.name, list_path(list) %>
    </div>
  <% end %>
</div>
