<div class="container">
  <h1 class="text-center mt-3">Add a new list</h1>

  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.submit "Add this movie list", class: "btn btn-primary w-100" %>
  <% end %>

</div>
