<div class="container">
  <h1 class="text-center mt-3">Add a new bookmark</h1>

  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.submit "Add this movie", class: "btn btn-primary w-100" %>
  <% end %>

</div>
